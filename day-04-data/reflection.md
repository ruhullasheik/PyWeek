# Reflection

1. When would you use a list vs a tuple vs a set?
2. What's the time complexity of `x in list` vs `x in set`?
3. Why is `defaultdict` better than checking `if key in dict` manually?
4. How would you deduplicate a list while preserving order?
5. What does `lst[:]` do? Why might you use it?
