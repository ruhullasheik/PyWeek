"""Exercise 2: Number Guessing Game

The program picks a random number between 1 and 100.
The user has to guess it.

- After each guess, tell the user "too high" or "too low"
- Count the number of attempts
- When they get it right, print: "You got it in X attempts!"
- Limit to 7 attempts (binary search should find it in 7)

Use: import random; target = random.randint(1, 100)
"""
