# Reflection

1. Why does Python not have a C-style `for (;;)` loop?
2. When would you use `enumerate()` vs `range(len(...))`?
3. What problem does the `for...else` construct solve that would be harder without it?
4. How does Python's truthiness differ from C's (where 0 is false, non-zero is true)?
5. Why is there no `switch` keyword in older Python? (Python only got `match` in 3.10)
