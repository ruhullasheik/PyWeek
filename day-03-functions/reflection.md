# Reflection

1. What problem does the mutable default argument pitfall cause?
2. When would you use `*args` vs `**kwargs`?
3. How is a lambda different from a regular `def` function?
4. What does the `nonlocal` keyword do in a closure?
5. Why would you use a decorator instead of just calling a wrapper function manually?
