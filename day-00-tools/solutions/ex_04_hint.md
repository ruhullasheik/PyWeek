# Hints for ex_04

```bash
mkdir uv-demo
cd uv-demo
uv venv

# Activate (varies by OS/shell):
# Windows PowerShell: .venv\Scripts\Activate.ps1
# macOS/Linux bash:   source .venv/bin/activate

uv pip install requests
uv pip list
deactivate
```
