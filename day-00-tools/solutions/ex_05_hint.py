"""Hints for ex_05

You'll need: os, sys, shutil (or subprocess)

    import os
    import sys
    import shutil

    # Current directory
    os.getcwd()

    # Python version
    sys.version

    # Platform
    sys.platform

    # PATH
    os.environ.get("PATH", "").split(os.pathsep)

    # Check if git exists
    git_path = shutil.which("git")
"""
