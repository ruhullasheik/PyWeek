"""Exercise 4: uv Demo

1. Create a new directory: `uv-demo`
2. Inside it, create a virtual environment using `uv venv`
3. Activate the environment
4. Install the `requests` library using `uv pip install requests`
5. Run: `uv pip list` and capture the output
6. Deactivate the environment

Paste the output of `uv pip list` as a comment below.
"""
