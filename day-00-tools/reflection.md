# Reflection

1. What's the difference between `python` and `python3` on your system?
2. Why use `uv` instead of plain `pip`?
3. What does `git status` tell you that `ls` doesn't?
4. Why is each day a separate branch? What problem does this solve?
5. What terminal command will you use most this week?
