# Reflection

1. What's the difference between `import os` and `from os import *`? Why avoid the latter?
2. What problem does `if __name__ == "__main__"` solve?
3. How is a Python package different from a module?
4. Why use uv over pip? What problem does it solve?
5. What's in your "most used stdlib modules" list after today?
