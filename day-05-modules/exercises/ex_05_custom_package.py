"""Exercise 5: Create Your Own Package

Create a package called `pyweek_utils` with the structure:

pyweek_utils/
├── __init__.py
├── strings.py            # reverse(), count_vowels(), is_palindrome()
└── numbers.py            # is_prime(), gcd(), factorial()

Then write a script that imports from your package and:
1. Checks if "racecar" is a palindrome
2. Reverses "PyWeek"
3. Counts vowels in "Hello World"
4. Computes GCD of 48 and 18
5. Checks if 17 is prime

Import from your package using relative or absolute imports.
"""
