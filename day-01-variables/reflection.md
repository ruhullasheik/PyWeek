# Reflection

1. Why can you assign `x = 5` and then `x = "hello"` in Python but not in C/Java?
2. What's the difference between `==` and `is` in Python? (Hint: try `a = 1000; b = 1000; a is b`)
3. Why is there no `++` operator in Python?
4. What does it mean that strings are "immutable"?
5. When would you use f-strings over `.format()` or `%` formatting?
