# Reflection

1. Why does Python require explicit `self` parameter but Java/C++ don't?
2. When would you use `@property` instead of plain getter/setter methods?
3. What's the difference between `@staticmethod` and `@classmethod`?
4. What problem do dataclasses solve?
5. Why are generators more memory-efficient than lists for large sequences?
6. What does a context manager guarantee that a try/finally block doesn't?
